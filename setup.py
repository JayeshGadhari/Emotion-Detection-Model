from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ml model used to detect emotion from text',
    author='jayesh',
    license='',
)
